import React from 'react'
import ReactDom from 'react-dom'

import App from './App'

ReactDom.render(<App/>, document.querySelector('#root')) //tout ce qu'on creer est
//dans App et avec querySelector on le met dans le div attribut root
